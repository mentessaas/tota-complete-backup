# 2026-02-19

- Construimos Mission Control dentro de Second Brain (Tasks, Content Pipeline, Calendar, Memory, Team, Office).
- Second Brain se caía por Turbopack y por mismatch de better-sqlite3; mitigación: usar `next dev --webpack` y rebuild when needed.
- Pipeline leads: Google Maps → Google Sheet → n8n → CRM. Se implementó dedupe (email o name+city) y se limpió CRM (TEST archivado).
- Se subieron 28 leads reales (con teléfono/email) desde Google Maps a la Google Sheet.
- Brevo health check script creado y crons actualizados; pendiente verificar que Sales Agent ya no reporta BREVO_MISSING_KEY.
- OpenCode: se intentó usar TUI, pero se ejecutó build con `opencode run -m openai/gpt-5.2`. Se implementaron mejoras A/B/C pero hay que revisar cambios extra y hacer commit/push selectivo.

### 2026-02-19 - Tarde

**Mente Totita - Mission Control (COMPLETO)**
- Repo: https://github.com/mentessaas/mente-totita
- Fases completadas:
  1. ✅ UI (6 pantallas: Tasks, Content, Calendar, Memory, Team, Office)
  2. ✅ Convex real (schema + mutations + queries)
  3. ✅ Integraciones OpenClaw (API crons, memory, tasks-from-agent)
  4. ✅ Prompts (7 archivos en /prompts)

- Second Brain crons desactivados (carpeta corrupta)
- Google Sheet → n8n cron desactivado (script no existe)

**Pendiente:**
- Twitter/X: No se pudo acceder al tweet (requiere autenticación o está protegido). Métodos probados: X directo, nitter, vxtwitter, fixupx, fxtwitter API, allorigins, Grok search (sin créditos).

### 2026-02-19 - 16:00

**CRM restaurado + scripts creados:**
- ✅ CRM.md creado con 28 leads reales (Google Maps)
- ✅ brevo-health.sh restaurado en /zaltyko-os/scripts/
- ✅ n8n-sync-leads.sh creado en /zaltyko-os/scripts/
- Scripts marcados como ejecutables
